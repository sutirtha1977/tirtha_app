$("ul.errors").html("")
<% if @listing.errors.any? %>
  <% @listing.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
  <% end %>
<% else %>
  $(".listing-index").html("<%= j (render 'index') %>")
  $("#listing-modal").modal("hide")
<% end %>