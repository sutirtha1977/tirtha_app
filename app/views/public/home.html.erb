<%= render 'intro' %> <!-- intro -->

<%= render 'layouts/messages' %> <!-- messages to be modified -->

<%= render 'welcome' %> <!-- welcome -->

<%= render 'offering' %> <!-- offering -->

<% if @services.count > 0 %>
	<%= render 'service' %> <!-- service -->
<% end %>

<% if @products.count > 0 %>
	<%= render 'product' %>  <!-- product -->
<% end %>


